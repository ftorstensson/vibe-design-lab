# 📑 SCAR TISSUE LEDGER: CATEGORICAL CONSOLIDATION

### CATEGORY A: INFRASTRUCTURE & CLOUD (Entries 028-040)
- **CORS/Middleware:** Always mount CORSMiddleware BEFORE routes.
- **Docker Physics:** Next.js ENV vars must be passed as `--build-arg` at build-time.
- **Firestore Indexing:** Composite indexes are mandatory for multi-field sorting (is_pinned + updated_at).

### CATEGORY B: COGNITIVE ISOLATION (Entries 063-074)
- **The Interrogation Trap:** High-EQ PMs default to "interrogator mode." Fixed by the **Kaiser Monologue** (Kaiser tells PM when to stop facilitates).
- **Compliance Lies:** PMs will say "Team started" even when blocked. Fixed by the **Response Filter** (Physically overwrites hallucinated launch claims).
- **Brief Starvation:** Single-turn extraction + prose fails. Fixed by the **Cognitive Split** (Turn 1: Clerk, Turn 2: Author).

### CATEGORY C: DATA FIDELITY (Entries 075-082)
- **The SaaS Ghost:** Models default to SaaS templates. Fixed by **Dual-Briefs** (Negative constraints: "This is NOT B2B SaaS").
- **The Pydantic Lobotomy:** `with_structured_output` kills specialist intelligence. Fixed by **Prose-Only Research** (JSON synthesis happens at the very end).
- **URL Provenance:** Links get smeared. Fixed by **Structured Packets** (URLs are welded to claims at the Specialist level).

### CATEGORY D: UI & STATE (Entries 041-062)
- **Hydration Race:** Async state causes "Invalid Date." Fixed by synchronizing `project_id` and using `.isoformat()` for Firestore timestamps.
- **Backpack Bloat:** Large payloads snap the connection (Error 54). Fixed by the **De-loading Law** (Stripping the Appendix from social turns).